name: Ukryj-Moja-Liste-Aplikacji

on:
  push:
    branches: [ PL ]
  pull_request:
  [workflow_dispatch]:

permissions:
  contents: write

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: 21
        distribution: 'temurin'
        cache: 'gradle'

    - name: Write key
      if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/PL' || github.event_name == 'workflow_dispatch')
      run: |
        if [ ! -z "${{ secrets.SIGNING_KEY }}" ]; then
        echo releaseStorePassword='${{ secrets.KEY_STORE_PASSWORD }}' >> gradle.properties
        echo releaseKeyAlias='${{ secrets.ALIAS }}' >> gradle.properties
        echo releaseKeyPassword='${{ secrets.KEY_PASSWORD }}' >> gradle.properties
        echo releaseStoreFile='key.jks' >> gradle.properties
        echo ${{ secrets.SIGNING_KEY }} | base64 --decode > key.jks
        fi

    - name: Create dummy local.properties
      run: touch local.properties

    - name: Build with Gradle
      run: ./gradlew assembleRelease assembleDebug

    - name: Rename APK
      run: |
        mv app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/HMAL.apk
        mv app/build/outputs/apk/debug/app-debug.apk app/build/outputs/apk/debug/HMAL-debug.apk

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: outputs
        path: app/build/outputs/apk

    - name: Upload to Releases
      if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/PL' || github.event_name == 'workflow_dispatch')
      uses: ncipollo/release-action@v1
      with:
        artifacts: "app/build/outputs/apk/release/HMAL.apk,app/build/outputs/apk/debug/HMAL-debug.apk"
        tag: "v4.2.2.219-219"
        name: "HMAL-4.2.2.219-(219)"
        allowUpdates: "true"
        token: "${{ secrets.GITHUB_TOKEN }}"